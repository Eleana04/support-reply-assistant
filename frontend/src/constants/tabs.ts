import { strings } from "./strings";

export const TABS = [
  { id: "new", label: strings.tabs.newTicket },
  { id: "archive", label: strings.tabs.archive },
  { id: "metrics", label: strings.tabs.metrics }
];